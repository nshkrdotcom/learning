Now let's update the top-level README to reflect the expanded scaffold and revised priorities:Good, the content is identical to what I had — must have been a hidden character mismatch. Let me retry with the exact text now confirmed.Now let's also add a research-log entry capturing this session's revisions, re-zip, and verify everything:Here's what I think this needs, ranked by leverage. I've already revised the scaffold to match (six new decisions, two new experiments, a revised E001 — re-zipped above), so this is the reasoning behind those edits, not just talk.

**Do this today, for free, before writing any more code.** `pythia-70m-deduped` SAEs are already hosted on Neuronpedia (`p70d-sm` release, `res-sm` variants across layers 0–5, same `blocks.N.hook_resid_post` hook convention you're already using) — I confirmed this rather than assuming it. Look up your two existing Phase 2 features right now: `neuronpedia.org/pythia-70m-deduped/2-res-sm/12300` and `.../25521`. If either has a public auto-interp label already, that's a free, independently-sourced sanity check on your own causal result. This also means you don't need to swap models for credibility — I'd considered recommending GPT-2-small for this reason and walked it back once I confirmed Pythia's already covered (decision D012 in the log explains why GPT-2-small's `res-jb` SAEs are `hook_resid_pre`, not `hook_resid_post`, which would actually fight your existing fail-closed compatibility checker).

**The thing I'd fix before anything else gets built (E000).** Your Phase 3 spec computes `intended_direction_pass_rate` but treats it as one threshold buried inside a big report. Small models are documented to handle negation unreliably at the behavioral level — Kassner & Schütze's negated-probes work is the classic citation. If Pythia-70m-deduped doesn't even prefer the right token under negation with *no* intervention, every SAE feature result downstream is explaining noise, and you won't find that out until after building baselines, telemetry, and the report schema. Run the no-intervention baseline check first, by itself, and look at the number before writing `baselines.py`. I added `E000` as a hard prerequisite.

**Add one trivially-easy positive-control task family.** Something like a strongly-primed factual completion that small LMs reliably get right. Without it, a null result on negation is ambiguous — "negation isn't represented this way" and "this harness can't detect anything, ever" look identical. With it, a negation null becomes a real, reportable finding instead of an inconclusive one. This is the single highest-leverage change for making a *negative* result legitimate.

**Replace the 3-random-seed ratio with an actual empirical null.** `min_top_vs_control_ratio: 1.05` over 3 seeds is exactly the kind of statistic that gets shredded in this community's comment sections. Running ~30–50 seeded random feature sets and reporting the top set's percentile rank within that distribution is barely more expensive on a 70M CPU run and converts "beats three random sets" into a defensible randomization-test claim. Add a paired test (sign test or Wilcoxon) across tasks alongside it, since you already have per-task rows — comparing only aggregate means throws that away.

**The gap I'd actually call out: nothing currently tests a "self" anything.** Phase 3 as specified compares feature-*selection* methods (top vs. random vs. bottom-active) — valuable, but it's external validity testing, not self-explanation grounding. The project's name promises a prediction scored against an intervention, and right now nothing does that. I added `E002`: take the top-activating examples you already produce, get a structured causal prediction from a model that's actually big enough to produce one (Pythia-70m can't), save that prediction with a timestamp *before* comparing it to the real Phase 2/3 numbers, then score sign-match. One caveat I built into the design: this has to be an offline, recorded annotation step, not a live API call wired into the pipeline — your own Phase 3 hard rules already forbid hidden network calls, and determinism matters more here than convenience. This is also the most novel, most citable piece if it works *or* if it comes back a clean null (a null here would directly corroborate McCann's descriptive-collision finding with your own data).

**Stretch, once the above is solid:** a layer sweep across the already-available 0–5 layers of the same SAE release (cheap, standard mech-interp move, makes a good figure) and GPT-2-small as a cross-model replication target later, not now.

**The part that actually gets you noticed, separate from the paper.** This community reads LessWrong/Alignment Forum posts, not arXiv PDFs. I added `portfolio/WRITEUP_PLAN.md` as a distinct document from `paper/draft.md` — different structure (epistemic status line, TL;DR, "what would change my mind," limitations section that reads as self-critical rather than defensive), built around figures you don't have yet but should plan for now: the calibration bar chart, the empirical-null histogram with your top set's position marked, per-family small-multiples instead of one aggregated number. Tag the exact commit you write about — don't just say "main branch."

The diagram above is the revised pipeline shape — the two boxes that weren't in your original Phase 3 plan are the feature-set-baselines box (now an empirical null, not three seeds) and the intervention box (now scored against a prediction, not just measured).

Net effect on the actual build order: **E000 → E001's task/ranking code → (E001's intervention code in parallel with E002's labeling)**, not "build the whole Phase 3 spec, then see what the numbers say."
