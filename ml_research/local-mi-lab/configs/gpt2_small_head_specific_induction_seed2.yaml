experiment:
  name: gpt2_small_head_specific_induction_seed2
  seed: 2

model:
  name: gpt2-small
  backend: transformer_lens
  device: cuda
  dtype: float32
  use_inference_mode: true

resources:
  max_disk_cache_gb: 100
  max_activation_cache_gb_per_run: 20
  max_ram_gb_per_run: 64
  max_gpu_vram_fraction: 0.80
  max_examples_initial: 192
  max_examples_full: 1024
  batch_size_auto: true

task:
  name: induction_controls
  prompt_style: controlled_repeated_token_sequences
  n_examples_initial_per_family: 32
  families:
    - positive_repeat_sequence
    - no_repeat_control
    - shuffled_repeat_control
    - distractor_repeat_control
    - same_token_frequency_control
    - random_expected_token_control

head_specific:
  layers: auto_even_6
  families:
    - positive_repeat_sequence
    - distractor_repeat_control
    - random_expected_token_control
    - same_token_frequency_control
  examples_per_family: 8
  metric: true_vs_control_logit_diff
  intervention: head_clean_to_corrupt_patch
  position: final
  include_random_comparison_heads: true
  include_raw_attention_candidates: true
  include_all_selected_layer_heads: true

outputs:
  run_root: runs
